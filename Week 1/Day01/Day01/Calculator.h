#pragma once
class Calculator
{
public:
	//data members
	//variable (field)
	int currentNumber;

	//member functions (or methods)
	//the declaration of the sum method
	//methods (or functions)
	int minus(int number1, int number2);//declaration. NO { }

	int Add(int , int );

	static double mult(double num, double factor);
};

